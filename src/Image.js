import { useRef } from 'react'

const Image = () => {
    const pokeons = [

    ];
};

return (
    <div>
    
        pokemons.map((pokemon,index,array) => {
            return (
            <img src={'./pokedex_assets/${pokemon}.png '}></img>);
            } );
        
    
    
    </div>
);